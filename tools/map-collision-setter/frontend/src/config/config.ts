import {IApiConfig} from "@/interfaces/config/config.interface";

export const API_CONFIG: IApiConfig = {
    // TODO:
    // use .env file
    // url: `${process.env.VUE_APP_API_URL || 'http://localhost:4001'}/api/`
    url: 'http://localhost:4001/api/'
}
