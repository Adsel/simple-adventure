{
  "name": "simple-adventure",
  "version": "1.0.0",
  "description": "Simple MMO RPG pixel game based on websocket.",
  "engines": {
    "node": ">=22.0.0 <23",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "client:serve": "cd ./client && npm run serve",
    "client:build": "cd ./client && npm run build",
    "server:serve": "cd ./server && npm run serve",
    "server:build": "cd ./server && npm run build",
    "client:dev": "cd ./client && npm run serve",
    "server:dev": "cd ./server && npm run dev",
    "lint": "npm run lint --workspace server && npm run lint --workspace client",
    "lint:fix": "npm run lint:fix --workspace server && npm run lint --workspace client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Adsel/simple-adventure.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Adsel/simple-adventure/issues"
  },
  "homepage": "https://github.com/Adsel/simple-adventure#readme",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ]
}
